/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["logodix.com", "seeklogo.com", "images.unsplash.com"], // Add any other domains as needed
  },
};

export default nextConfig;
